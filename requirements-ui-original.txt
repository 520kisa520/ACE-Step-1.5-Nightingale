# 原版 ACE-Step Gradio UI 本地预览依赖（安装到项目 .venv）
# Mac Apple Silicon 适用；仅看界面布局，不加载 GPU 模型时可不加 --init_service

gradio==6.2.0
loguru>=0.7.3
python-dotenv
transformers>=4.51.0,<4.58.0
diffusers>=0.37.0
accelerate>=1.12.0
einops>=0.8.1
soundfile>=0.13.1
scipy>=1.10.1
matplotlib>=3.7.5
peft>=0.18.0
diskcache
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
toml
modelscope
pyyaml
httpx
huggingface_hub
safetensors
numpy
pillow

# PyTorch (macOS ARM / MPS)
torch>=2.9.1
torchvision
torchaudio>=2.9.1

# Apple Silicon 可选加速（原版 UI 导入链可能用到）
mlx>=0.25.2; sys_platform == "darwin" and platform_machine == "arm64"
mlx-lm>=0.20.0; sys_platform == "darwin" and platform_machine == "arm64"
